using System;
namespace DataTemplateRepeaterControl.ViewModels
{
    public class RepeaterItemsViewModel : ListItemsViewModel
    {
        // We just need different classes for view models, not actually different code
    }
}
